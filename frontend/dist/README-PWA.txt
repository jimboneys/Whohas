WhoHas — PWA build
Backend: https://whohas-app.emergent.host
Host the contents of this folder at the site ROOT over HTTPS (e.g. Vercel).
vercel.json (SPA rewrites + PWA headers) is included.
Rebuild against another backend:  ./scripts/build_pwa.sh https://your-backend-url
